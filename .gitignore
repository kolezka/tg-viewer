# Telegram backup data (contains personal messages)
tg_*/
test-data/
telegram_keys.json
decrypted_data/
parsed_data/
*.tar.gz

.tempkeyEncrypted
accounts-shared-data
accounts-metadata/
notificationsKey

# Database files
*.db
*.sqlite
*_export.json

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
*.egg
.Python
venv/
env/
.venv/

# IDE/Editor
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
._*
.Spotlight-V100
.Trashes
Thumbs.db

# Logs
*.log

# Claude Code
.claude/
.worktrees/

# Superpowers skill artifacts (planning docs, specs)
docs/superpowers/

# Playwright MCP cache
.playwright-mcp/

# Environment
.env
.env.*

# Phase 2b web frontend
apps/web/node_modules/
apps/web/dist/
apps/web/src/styles.generated.css
tg_continuous/
tg_vault/

# launchd .app bundles are generated locally by `./tg-viewer install-launchd`
# (paths in their launcher scripts are absolute to this checkout, so they
# don't transfer between machines anyway).
apps/launchd-bundles/

# graphify knowledge graph — generated output (graph.json/html, wiki, report).
# Rebuilt locally by `graphify` + the post-commit hook; not meant to be tracked.
graphify-out/
